# Only needed when creating the admin account for the first time.
PZ_ADMIN_PASSWORD=

# Java heap limit for this host. Leave empty to use the game's default.
PZ_JAVA_XMX=4g

# Optional host directory for Compose backups. It must already exist.
# PZ_BACKUP_DIR=/home/potsonhumer/pz-backup
